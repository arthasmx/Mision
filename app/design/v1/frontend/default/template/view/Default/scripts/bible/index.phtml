<?php
  $iteration = 0;
  $testaments = array(
                      'old' => array('left',  20  ),
                      'new' => array('right', 53 )
                      );
?>

  <div class="all-panel">
    <div id="bible">

      <?php foreach($testaments AS $key => $testament): ?>

        <div id="bible-<?=$testament['0']?>">
          <h1><?=App::xlat('BIBLE_testament_' . $key )?></h1>
          <ul>

            <?php foreach($this->books AS $book): ?>
              <?php if( $key != $book['testament'] ): ?>
                </ul>
                <?php break; ?>
              <?php endif; ?>
              <?php if( $iteration == $testament['1'] ): ?>
                </ul><ul>
              <?php endif; ?>

              <li> <a href="<?=App::base( App::xlat('route_bible') . $book['seo'] ) ?>"> <?=$book['book']?> </a> </li>

              <?php array_shift($this->books); ?>
              <?php $iteration++; ?>
            <?php endforeach; ?>

          </ul>
        </div>

      <?php endforeach; ?>

    </div>
    <?=App::module('Addons')->renderBlock("bible/index","options", array("section" => "bible") )?>
    <?//App::module('Addons')->renderBlock("bible/index","search", array("form_type" => "advanced") )?>
  </div>

<?php
/*
$this->books
Array
(
    [0] => Array
        (
            [book_id] => 1
            [book] => Génesis
            [seo] => genesis
            [testament] => old
        )
*/
?>
